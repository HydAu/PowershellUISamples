<?xml version='1.0' encoding='UTF-8'?>
<project>
  <actions/>
  <description></description>
  <logRotator class="hudson.tasks.LogRotator">
    <daysToKeep>-1</daysToKeep>
    <numToKeep>1</numToKeep>
    <artifactDaysToKeep>-1</artifactDaysToKeep>
    <artifactNumToKeep>-1</artifactNumToKeep>
  </logRotator>
  <keepDependencies>false</keepDependencies>
  <properties/>
  <scm class="hudson.scm.NullSCM"/>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <triggers/>
  <concurrentBuild>false</concurrentBuild>
  <builders>
    <hudson.plugins.groovy.Groovy plugin="groovy@1.18">
      <scriptSource class="hudson.plugins.groovy.StringScriptSource">
        <command>// this is how to load the specific jar to the CLASSPATH at runtime 
// instantiating class that is no longer in the default Groovy JAR
// apache.commons.cli would be a good class 
// example how to load ... when the runtime is not the groovy script tool but the jenkins runtime
// unfinished
// this.getClass().classLoader.rootLoader.addURL(new File(&quot;D:/java/groovy-2.3.2/embeddable/groovy-all-2.3.2.jar&quot;).toURI().getPath()))
// manager.hudson.class.classLoader.addURL(new URL(&quot;file:///&quot; + filePath))
// println(Jenkins.instance.pluginManager.plugins)

// f = new File(MyClass.class.getProtectionDomain().getCodeSource().getLocation().toURI().getPath());
f = this.getClass().classLoader.rootLoader
println(f.getClass() )
z = f.addURL(new File(&quot;file.jar&quot;).toURL())
println(z.getClass() )

def context = Class.forName(klass)
def absolutePath = context.getResource(&apos;/&apos; + context.name.replace(&quot;.&quot;, &quot;/&quot;)
           + &quot;.class&quot;).getPath()</command>
      </scriptSource>
      <groovyName>2.3.2</groovyName>
      <parameters></parameters>
      <scriptParameters></scriptParameters>
      <properties></properties>
      <javaOpts></javaOpts>
      <classPath></classPath>
    </hudson.plugins.groovy.Groovy>
  </builders>
  <publishers/>
  <buildWrappers/>
</project>
